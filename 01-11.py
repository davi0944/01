str = input()
str.replace(" ", "")
print (str)
以上代码输出的字符串仍然带有空格，是因为str.replace(" ", "")这行代码只是对str变量中的空格进行替换，并没有修改到原来的str变量。在Python中，字符串是不可变的，即一旦创建就不能修改。所以，str.replace(" ", "")返回的是一个新的字符串，只是没有将新字符串赋值给str变量。

要解决这个问题，你可以将返回的新字符串赋值给str变量，这样输出的字符串将不再带有空格。以下是修改后的代码示例：

str = input()
str = str.replace(" ", "")
print(str)
或者，你也可以直接将str.replace(" ", "")的返回值直接传递给print()函数，实现一行代码完成字符串的输入和输出：

print(input().replace(" ", ""))
这样就能实现将输入的字符串中的空格去除，并输出不带空格的字符串。